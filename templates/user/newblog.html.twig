{% extends 'base.html.twig' %}

{% block title %}new Blog{% endblock %}

{% block body %}
<div class="card mt-4 mb-3">
  
  <div class="card-body">
    <h5 class="card-title">Create your new Blog here</h5>In title put the subject of your Blog and in description make a fast description of your problem .</p>
  </div>
</div>

{{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-primary">{{ button_label|default('create the Blog') }}</button>
    {{ form_end(form) }}
{% endblock %}
